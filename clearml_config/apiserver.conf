auth {
    # Fixed users — only these accounts can log in.
    # Change username/password before deploying.
    fixed_users {
        enabled: true
        pass_hashed: false
        users: [
            {
                username: "admin"
                password: "clearml1234"
                name: "Admin"
            },
        ]
    }
}

apiserver {
    files {
        default_storage {
            fileserver: "http://fileserver:8081"
            base_url: "http://fileserver:8081"
        }
    }
}
